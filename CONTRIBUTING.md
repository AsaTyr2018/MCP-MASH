# Contributing

Keep changes scoped and include tests or a clear manual verification path for MCP behavior, script validation, scheduler behavior, and Mailbridge adapter boundaries.

Never commit runtime data from `./data`, `.env`, bearer tokens, Mailbridge automation tokens, private mail data, generated private scripts, or run logs.

Security-sensitive changes should explain account scoping, token handling, and what gets written to logs.
